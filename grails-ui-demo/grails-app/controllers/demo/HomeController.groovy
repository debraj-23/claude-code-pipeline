package demo

class HomeController {

    /**
     * Default landing page after login — shows Merchant tab (empty grid)
     */
    def index() {
        // nothing to load — just render the Merchant tab view
    }
}
