package com.demo.mpmbackend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class MpmBackendApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring application context starts (security, JPA, seed data).
    }
}
